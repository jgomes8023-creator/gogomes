# Scripts for SoundCanvas Setup

This folder contains platform-specific scripts for installing and running SoundCanvas:

- `install.sh`: Installer for Linux/macOS systems.
- (Future) Scripts for Windows installer (Setup.exe), Android, and iOS releases.